# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"

gem "jekyll", "~> 3.8"
gem "kramdown-parser-gfm"
gem 'wdm', '>= 0.1.0' if Gem.win_platform? && RUBY_VERSION < '4.0'
gem "webrick"
gem "base64"
gem "bigdecimal"